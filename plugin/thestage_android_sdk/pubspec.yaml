name: thestage_android_sdk
description: "TheStage AI SDK Flutter plugin (Android)."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.5.0
  flutter: ">=3.3.0"

flutter:
  plugin:
    platforms:
      android:
        package: ai.thestage.thestage_android_sdk
        pluginClass: TheStageFlutterPlugin

dependencies:
  flutter:
    sdk: flutter
