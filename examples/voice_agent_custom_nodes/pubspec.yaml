name: voice_agent_custom_nodes
description: "Voice Agent Custom Nodes — TheStage AI Android SDK (extraNodes, VLM captions, screen record)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  thestage_android_sdk:
    path: ../../plugin/thestage_android_sdk
  image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
